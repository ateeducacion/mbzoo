# Fixture manifest
# Fixtures are deterministic; regenerate with:
#   bun run fixtures/scripts/generate-fixture.ts
# If a checksum changes unexpectedly, treat it as a parser/generator regression.
fixtures:
  demo-course-zip.mbz:
    sha256: 136d9f735799f30f334754ee8175e4272ca1ff807ede4417b8bda919fdfdd5f9
    bytes: 27649
    format: zip
    synthetic: true
    contains_user_data: false
    provenance: generated by fixtures/scripts/generate-fixture.ts (this repo)
    moodle_version_target: "3.8 schema shapes"
    includes:
      sections: 3
      activities: [page, label, page(content), quiz(2 questions), glossary(2 entries), resource, assign, supermodule(unknown-plugin), h5pactivity(synthetic .h5p: nested libs, string versions, content/ image), feedback(label + multichoice + pagebreak + textarea), lesson(branching: content page + question page), choice(2 options), forum(qanda, no discussions), data(2 fields), workshop(instructions + example), imscp(PHP-serialized TOC), url, folder(text + 8-bit WAV tone), chat, wiki, survey(retired 5.0), assignment(retired 4.2), subsection(delegated section owning a page), scorm(SCORM 1.2: organization + 2 launchable SCOs, content tree + package zip)]
      grading: [assign grade item (max/pass/weight), assign rubric (2 criteria), course gradebook (2 categories, 2 letters)]
      packages: [epub(EPUB 3: 2-chapter spine + relative css/image), elpx(eXeLearning 4: content.xml + exported 2-page site)]
      files_indexed: 12
    expected_parser_result:
      course_fullname: Demo Course for MBZoo
      course_shortname: DEMO-101
      warnings: []
  # Real-world verification files (REPO-004) are NOT committed. Record any
  # downloaded specimen here with provenance + sha256 when used.
external:
  mbzoo-specimen-moodle-5.2.2.mbz:
    sha256: cae25c164993dbcec2e834dd6a542e2baef36cac70a70e77c0722cc2ac762bb1
    source: >-
      Generated locally, not downloaded: a course built with Moodle's own APIs
      inside erseco/alpine-moodle (Moodle 5.2.2, SQLite), backed up through
      backup_controller with users=0. Holds lesson (branching pages + answers),
      choice, data, workshop (+ example submission), assign (+ rubric),
      subsection (delegated section), qbank, lti, url/page/folder with display
      modes, forum, and a full gradebook.xml.
    committed: false
    reason: >-
      Regenerable from the recipe above; a backup is not fixture material we
      keep. Used to verify the parsers built from the Moodle schema against a
      real Moodle's output — which is how the lesson jump-target bug was found.
  moodle-mod_subsection-test-fixture.mbz:
    sha256: 50cf0f82b38a07a13b23a1beb5c27e7c72b4a6988ee5d6084d26394d7ce6eaac
    source: >-
      moodle/moodle public/mod/subsection/tests/fixtures/subsections_with_descriptions.mbz
      (REPO-005), read from a local checkout at 6216fe4ed19.
    committed: false
    reason: "GPL-3.0 upstream; used locally to verify delegated-section resolution."
  moodle-backup_controller-deadlock.mbz:
    sha256: cedd1556829d2e92df77103b75bb6834f18f49c452a8a05c47d06c3cf9985ca9
    source: >-
      moodle/moodle public/backup/controller/tests/fixtures/deadlock.mbz
      (REPO-005) — carries a real IMS content package.
    committed: false
    reason: "GPL-3.0 upstream; used locally to verify the imscp TOC parser."
  prdv103-2017-07-21-mbz.mbz:
    sha256: 77df2b341aa044bea7cfcec615aa7698a486b99d68f1898a8f47e076df6f12cc
    sha256_note: >-
      Corrected 2026-08-25. The previously recorded 9756c118…600a does not
      match the file at this URL; two independent downloads (gh contents API
      and raw.githubusercontent) agree on 77df2b34… for 43342 bytes.
    source: https://github.com/saylordotorg/course_backups/blob/master/prdv103/PRDV103-2017-07-21-mbz.mbz (REPO-004)
    committed: false
    reason: "no LICENSE file upstream; embedded third-party content under varied licenses (see compliance/licensing)"
